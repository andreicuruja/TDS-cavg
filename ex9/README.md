cacetada como github é difícil de usar por terminal no vai se foder

git init

git clone https://github.com/andreicuruja/secret.git

cd .\secret\

git branch -a

git checkout qt

git status

git add .

git commit -m "textoMensagemDoUpdateDoCommit"

git push origin qt

("em caso de cagada extrema utilizar:

git pull origin qt --allow-unrelated-histories

                                                ").